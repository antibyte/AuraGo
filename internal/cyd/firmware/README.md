# Bundled Cheap Yellow Display firmware

Fixed path used by the config web flasher: `internal/cyd/firmware/cyd/`.

Refresh from a PlatformIO build of aurago-cyd:

```
agocyd/firmware/cyd/{bootloader,partitions,boot_app0,firmware}.bin
agocyd/firmware/cyd/version.txt
```
