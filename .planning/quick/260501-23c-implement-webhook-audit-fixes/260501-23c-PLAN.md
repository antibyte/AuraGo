# Quick Task 260501-23c: Implement webhook audit fixes

**Date:** 2026-04-30
**Status:** In progress

## Scope

Implement the confirmed webhook audit fixes with focused tests and documentation updates:

1. Harden incoming webhook execution paths.
   - Bind incoming webhook tokens to the configured webhook `token_id`.
   - Enforce Guardian and LLM Guardian block decisions before delivery.
   - Fix internal message delivery authentication.
   - Produce valid SSE JSON for notification delivery.
   - Use constant-time comparison for plain signatures.

2. Enforce management safety and reliable persistence.
   - Honor `webhooks.readonly` in server-side mutating handlers.
   - Preserve partial update semantics for omitted boolean/signature fields.
   - Keep `rate_limit: 0` as unlimited.
   - Use atomic writes for webhook state/log persistence.
   - Serialize outgoing form payloads.

3. Fix UI/docs contracts and tests.
   - Align preset placeholders with the current prompt-template syntax.
   - Fix UI test-preview and outgoing-panel refresh contracts.
   - Mask outgoing webhook secrets in API/tool list output.
   - Update webhook tool manual.
   - Add focused regression tests for the security and reliability fixes.

## Verification

- `go test ./internal/webhooks ./internal/server ./internal/tools ./internal/agent -run "Webhook|Webhooks|Token|Outgoing"`
- `go vet ./internal/webhooks ./internal/server ./internal/tools ./internal/agent`
- Secret scan of the final diff before commit.