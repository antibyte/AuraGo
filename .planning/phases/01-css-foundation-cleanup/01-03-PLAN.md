---
phase: 01-css-foundation-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/shared.css
autonomous: true
requirements:
  - CSS-04
must_haves:
  truths:
    - "CSS naming convention is documented in shared.css header"
    - "New component classes use the .aura-* prefix convention"
  artifacts:
    - path: "ui/shared.css"
      provides: "CSS naming convention documentation"
  key_links:
    - from: "shared.css header"
      to: "All new CSS"
      via: "Documented convention"
      pattern: "\\.aura-"
---

<objective>
Document the .aura-* prefixed naming convention in shared.css header comments and create a CSS coding standards section that explains naming rules for all contributors.

Purpose: Per CSS-04 (establish naming convention) and D-01 (use prefixed naming for new component classes).
Output: shared.css with documented naming convention; no code changes, only comments added.
</objective>

<context>
@ui/shared.css (lines 1-4) -- Already has a header. Need to expand it with a "CSS Naming Convention" section.
@01-CONTEXT.md D-01 -- Locked decision: Use .aura-* prefix for all new component classes. Examples: .aura-card, .aura-badge, .aura-btn. Existing classes (.card, .badge, .btn) remain as-is for backward compatibility.
@01-CONTEXT.md D-03 -- Split shared.css with section comments (keep file count low). Add section comments throughout.

This plan is documentation-only -- no CSS code changes, just header/comment additions to shared.css.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS naming convention to shared.css header</name>
  <files>ui/shared.css</files>
  <action>
Read the first 30 lines of shared.css to see the current header format.

Expand the header comment block (lines 1-4) to include a "CSS Naming Convention" section. Add 20-40 lines of documentation after the existing header.

Include:
1. "CSS Naming Convention" section header
2. Explain .aura-* prefix rule (per D-01): All NEW component classes MUST use .aura-* prefix
3. Examples of correct naming: .aura-card, .aura-badge, .aura-btn, .aura-modal, .aura-input
4. Examples of existing classes that are grandfathered: .card, .badge, .btn, .modal, .form-*
5. Why: BEM requires full rewrite; prefixed naming is pragmatic and backward-compatible
6. Pattern for modifiers: .aura-card (base) + .aura-card--highlighted (modifier) or .aura-card.highlighted (combined selector)
7. Note: New components added in Phase 2+ must follow this convention

Do NOT rename any existing classes -- this is documentation only.
  </action>
  <verify>
    <automated>grep -c "\.aura-" ui/shared.css</automated>
  </verify>
  <done>shared.css header contains documented naming convention with .aura-* examples</done>
</task>

<task type="auto">
  <name>Task 2: Add section comments throughout shared.css</name>
  <files>ui/shared.css</files>
  <action>
Read shared.css (all 3041 lines) to identify logical sections. Add clear section comment separators like:

/* ═══ RESET ═══════════════════════════════════════════════════════ */
/* ═══ THEME / DESIGN TOKENS ════════════════════════════════════════ */
/* ═══ COMPONENTS ════════════════════════════════════════════════════ */
/*   Buttons */
/*   Cards */
/*   Badges */
/*   Forms */
/*   Modals */
/*   Toggles */
/*   Toasts */
/*   Tabs */
/* ═══ UTILITIES ═════════════════════════════════════════════════════ */

Add these section dividers at logical break points. This is per D-03: "Split shared.css into logical sections (via comments, not separate files)."

Do NOT move any code -- only add comment dividers between existing sections.
  </action>
  <verify>
    <automated>grep -c "/\* ═══" ui/shared.css</automated>
  </verify>
  <done>shared.css has section dividers (at least 6 major sections with clear boundaries)</done>
</task>

</tasks>

<verification>
After all tasks:
- shared.css header contains documented .aura-* naming convention
- grep -c "/\* ═══" shared.css >= 6 (section dividers added)
- No CSS code was moved or changed (only comments added)
</verification>

<success_criteria>
1. shared.css header explains .aura-* prefix convention with examples
2. Section dividers added throughout shared.css to organize ~3000 lines
3. No existing CSS classes were renamed or moved
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-foundation-cleanup/01-03-SUMMARY.md`
</output>
