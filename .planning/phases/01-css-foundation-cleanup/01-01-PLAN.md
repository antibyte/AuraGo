---
phase: 01-css-foundation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/css/animations.css
  - ui/css/tokens.css
  - ui/css/missions.css
  - ui/css/invasion.css
  - ui/css/cheatsheets.css
  - ui/css/chat.css
  - ui/css/chat-modules.css
  - ui/css/containers.css
  - ui/css/config.css
  - ui/css/dashboard.css
  - ui/css/enhancements.css
  - ui/css/login.css
  - ui/css/skills.css
  - ui/css/stt-overlay.css
  - ui/css/knowledge.css
  - ui/css/truenas.css
autonomous: true
requirements:
  - CSS-01
must_haves:
  truths:
    - "All keyframe definitions exist in a single animations.css file"
    - "No duplicate keyframe definitions remain in any CSS file"
  artifacts:
    - path: "ui/css/animations.css"
      provides: "Centralized keyframe definitions"
      min_lines: 30
  key_links:
    - from: "ui/css/tokens.css"
      to: "ui/css/animations.css"
      via: "@import or removed definitions"
      pattern: "@keyframes fadeIn|pulse|card-enter"
---

<objective>
Extract all @keyframes definitions from every CSS file in ui/css/ into a single new file ui/css/animations.css, then remove the original definitions and replace them with references.

Purpose: Eliminate keyframe duplication (fadeIn, card-enter, pulse exist in 3+ files) per CSS-01.
Output: ui/css/animations.css with all unique keyframes; source files reference it or remove duplicates.
</objective>

<context>
@ui/css/tokens.css (lines 110-197) -- Already has fadeIn, fadeInUp, fadeInScale, card-enter, slideInRight, pulse, spin, bounce, float, voicePulse, shimmer, progress-stripes. These should be MOVED to animations.css and removed from tokens.css.
@ui/css/missions.css (lines 98, 370, 425) -- Has duplicate pulse, card-enter, fadeIn
@ui/css/invasion.css (line 37) -- Has duplicate card-enter
@ui/css/cheatsheets.css (line 121) -- Has duplicate fadeIn

Key finding: tokens.css already acts as a partial animation library but keyframes are mixed with CSS custom properties. The plan is to extract ALL @keyframes from ALL CSS files into animations.css.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan all CSS files for @keyframes</name>
  <files>ui/css/animations.css</files>
  <action>
Create ui/css/animations.css with a header comment explaining its purpose.

Extract and consolidate ALL @keyframes from ALL CSS files in ui/css/. Group by page/section in comments.

File contains these keyframes (collected from grep results):
- chat.css: msgIn, queueSlideIn, recorderSlideIn, recorderPulse, pulse-new, floatUp, statusIn, flashGreen, composerPopoverIn, typing, scaleIn, slideInRight, glowPulse, shimmer, bounceSoft, gradientShift
- config.css: pulse-glow-success, cfgRestartSpin
- containers.css: ctPulse, ctSlideIn, ctSlideOut
- dashboard.css: dashTabIn, pulse-glow, valueUpdate
- enhancements.css: typingBounce, voicePulse, statusPulse, messageSlideIn, panelFadeIn, valuePop, moodPulse, connectionPulse, toastSlideIn, bgShift, gentleFloat
- login.css: aurora-move, orb-float, grid-pulse, cardIn, glow-in, logo-pulse, scan
- skills.css: skToastIn
- stt-overlay.css: stt-ring-pulse, stt-mic-glow, stt-status-pulse, stt-blink
- knowledge.css: kcFadeIn
- truenas.css: tn-spin
- chat-modules.css: pulse-dot, vr-pulse
- tokens.css: fadeIn, fadeInUp, fadeInScale, card-enter, slideInRight, pulse, spin, bounce, float, voicePulse, shimmer, progress-stripes

Sort alphabetically within page groups. Use clear comment separators.

IMPORTANT: Do NOT invent or add any keyframes not found in the grep results. Only extract what is already defined.
  </action>
  <verify>
    <automated>grep -c "@keyframes" ui/css/animations.css</automated>
  </verify>
  <done>animations.css exists with all keyframes from grep results, organized by section comments</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate keyframes from tokens.css</name>
  <files>ui/css/tokens.css</files>
  <action>
Read tokens.css and remove the entire @keyframes block (lines ~105-197, the section labeled "ANIMATION KEYFRAMES"). 

After removal, tokens.css should only contain CSS custom properties (no @keyframes).

Do NOT add any @import for animations.css to tokens.css -- animations.css will be imported separately by the HTML pages or via shared.css import order.
  </action>
  <verify>
    <automated>grep -c "@keyframes" ui/css/tokens.css; grep -c "@keyframes" ui/css/animations.css</automated>
  </verify>
  <done>tokens.css has 0 @keyframes; animations.css has all the keyframes that were in tokens.css</done>
</task>

<task type="auto">
  <name>Task 3: Remove duplicate keyframes from other CSS files</name>
  <files>ui/css/missions.css, ui/css/invasion.css, ui/css/cheatsheets.css</files>
  <action>
For each of the following files, read the file, locate the @keyframes definition block, and replace it with a comment referencing animations.css:

missions.css:
- Remove @keyframes pulse (lines 98-101) -- replace with /* pulse keyframe moved to animations.css */
- Remove @keyframes card-enter (lines 370-376) -- replace with /* card-enter keyframe moved to animations.css */
- Remove @keyframes fadeIn (lines 425-428) -- replace with /* fadeIn keyframe moved to animations.css */

invasion.css:
- Remove @keyframes card-enter (lines 37-43) -- replace with /* card-enter keyframe moved to animations.css */

cheatsheets.css:
- Remove @keyframes fadeIn (lines 121-123) -- replace with /* fadeIn keyframe moved to animations.css */

For all other CSS files (chat.css, config.css, dashboard.css, login.css, enhancements.css, skills.css, stt-overlay.css, containers.css, knowledge.css, truenas.css, chat-modules.css): DO NOT remove their unique keyframes (msgIn, pulse-glow-success, aurora-move, etc.). These are page-specific and should stay in their respective files, NOT moved to animations.css. Only the duplicates (fadeIn, card-enter, pulse) get moved.
  </action>
  <verify>
    <automated>grep -l "@keyframes fadeIn\|@keyframes card-enter\|@keyframes pulse" ui/css/missions.css ui/css/invasion.css ui/css/cheatsheets.css 2>/dev/null | wc -l</automated>
  </verify>
  <done>missions.css, invasion.css, cheatsheets.css no longer contain duplicate fadeIn, card-enter, or pulse keyframes</done>
</task>

</tasks>

<verification>
After all tasks complete:
- grep -c "@keyframes" ui/css/animations.css should show a number > 25
- grep "@keyframes fadeIn" ui/css/*.css should only find animations.css (and any page-specific ones like msgIn that contain fadeIn in their name but are different animations)
- grep "@keyframes card-enter" ui/css/*.css should only find animations.css
- grep "@keyframes pulse" ui/css/*.css should only find animations.css (and page-specific ones like pulse-dot, pulse-new which are different animations)
</verification>

<success_criteria>
1. ui/css/animations.css exists with all shared keyframes
2. tokens.css contains zero @keyframes
3. missions.css, invasion.css, cheatsheets.css contain no duplicate fadeIn/card-enter/pulse keyframes
4. The animation-name values used in source files (e.g., animation: fadeIn 0.2s ease) still work because the keyframes now live in animations.css which is loaded via shared.css import order
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-foundation-cleanup/01-01-SUMMARY.md`
</output>
