# virtual_computers

Manage short-lived boring-computers microVMs through AuraGo's private Virtual Computers subsystem.

## Safety

- Requires `virtual_computers.enabled=true` and `tools.virtual_computers.enabled=true`.
- `virtual_computers.readonly=true` allows only status/list/get/screenshot/download operations and also blocks mutations forwarded to the management application.
- The boringd token is auto-generated by AuraGo during managed setup when missing, stored in the Vault, and never returned to the agent or browser. If connecting to an existing boringd, the Vault value must match that host's `BORING_TOKEN`.
- With `virtual_computers.enabled=true` and `virtual_computers.auto_setup=true`, AuraGo automatically installs or repairs both the pinned `boringd` control plane and the upstream Boring Computers management application at startup and after config hot reload. It reconciles the installed revision, cancels superseded setup work, and cleans up the management tunnel when disabled.
- The management application is available only through AuraGo at `/boring-computers/`. Its loopback listener on `127.0.0.1:18081` and boringd on `127.0.0.1:18082` must remain private.
- Drawer and setup-status health checks are bounded and passive; they must never be used to establish SSH tunnels. Tunnel creation belongs to background setup or an authenticated proxy request.
- Managed releases are immutable and use a private Node.js runtime below the integration install directory. Do not replace host-global Node.js commands or modify the active release in place.
- Preview and live channels must use AuraGo routes such as `/api/virtual-computers/machines/{id}/web/{port}/`; do not expose boringd directly.
- Tailscale access should expose AuraGo via `tailscale.tsnet.serve_http`, not boringd.
- `control_plane.mode=local_host` installs boring-computers on the same Ubuntu/Linux KVM host that runs AuraGo. It requires `/dev/kvm`, systemd, and root, passwordless sudo, or the central `sudo_password` secret in the Vault. AuraGo supplies that secret only through sudo standard input and redacts it from setup output. Saving this shared secret retries enabled local auto-setup immediately; removal remains a central Secrets-page operation because it also affects `execute_sudo` and package management. Docker, macOS/Lima, and Windows/WSL are not automated in v1.
- If an upstream error says boringd returned a non-JSON response, check that `virtual_computers.control_plane.boringd_url` points to boringd itself (normally `http://127.0.0.1:18082` behind AuraGo), not the AuraGo UI, a login page, or a redirect.
- A `503` from `/boring-computers/` means that the private management service or its SSH tunnel is not healthy. Check `/api/virtual-computers/setup/status`, then use the setup repair action; never expose ports `18081` or `18082` as a workaround.

## Operations

- `status`, `list_machines`, `get_machine`, `list_templates`
- `launch`, `destroy`, `extend`, `fork`, `exec`
- `screenshot`, `upload`, `download`
- `publish`, `list_volumes`, `create_volume`, `save_machine`
- `run_shell_task`, `run_desktop_task` when `virtual_computers.allow_agent_tasks=true`

TTL values are clamped to boringd's 15-900 second range and to `virtual_computers.max_ttl_seconds`.
