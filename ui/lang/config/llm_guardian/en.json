{
  "config.llm_guardian.enabled_label": "LLM Guardian enabled:",
  "config.llm_guardian.disabled_notice": "LLM Guardian is disabled",
  "config.llm_guardian.disabled_desc": "Enable to add an LLM-based security layer that evaluates tool calls before execution. Uses a fast, cheap model to detect suspicious operations. Falls back to the main LLM if no provider is selected.",
  "config.llm_guardian.provider_title": "Provider",
  "config.llm_guardian.provider_desc": "Select a provider for the Guardian LLM. A fast, cheap model (e.g. Gemini Flash, GPT-4o-mini, local 3B) is recommended for sub-second decisions.",
  "config.llm_guardian.provider_label": "Provider",
  "config.llm_guardian.select_provider": "— Main LLM (default) —",
  "config.llm_guardian.model_label": "Model",
  "config.llm_guardian.model_hint": "empty = provider default",
  "config.llm_guardian.level_title": "Protection Level",
  "config.llm_guardian.level_desc": "Controls which tool calls are sent to the Guardian for review. Higher levels check more operations but cost more tokens.",
  "config.llm_guardian.level_label": "Default level",
  "config.llm_guardian.level_off": "Off — No LLM checks",
  "config.llm_guardian.level_low": "Low — Only high-risk tools (shell, sudo, python)",
  "config.llm_guardian.level_medium": "Medium — All tools + external APIs",
  "config.llm_guardian.level_high": "High — Every tool call checked",
  "config.llm_guardian.failsafe_title": "Fail-Safe Behavior",
  "config.llm_guardian.failsafe_desc": "What happens when the Guardian LLM is unavailable or returns an error (timeout, rate limit, etc.).",
  "config.llm_guardian.failsafe_label": "On error",
  "config.llm_guardian.failsafe_block": "Block — Deny the operation (safest)",
  "config.llm_guardian.failsafe_quarantine": "Quarantine — Allow but log and warn",
  "config.llm_guardian.failsafe_allow": "Allow — Skip check (least safe)",
  "config.llm_guardian.perf_title": "Performance",
  "config.llm_guardian.perf_desc": "Cache and rate limiting settings to control costs and latency.",
  "config.llm_guardian.cache_ttl_label": "Cache TTL (seconds)",
  "config.llm_guardian.rate_limit_label": "Max checks per minute",
  "config.llm_guardian.overrides_title": "Tool Overrides",
  "config.llm_guardian.overrides_desc": "Override the protection level for specific tools. Useful to always check dangerous tools or skip safe ones.",
  "config.llm_guardian.overrides_tool_placeholder": "Tool name (e.g. execute_shell)",
  "config.llm_guardian.overrides_tool_search": "Search tool...",
  "config.llm_guardian.clarification_title": "Agent Clarification",
  "config.llm_guardian.clarification_desc": "When enabled, the agent can provide a justification if a tool call is blocked. The Guardian re-evaluates the request once with the justification before making a final decision.",
  "config.llm_guardian.clarification_label": "Allow agent clarification",
  "config.llm_guardian.scan_title": "Content Scanning",
  "config.llm_guardian.scan_desc": "Use the Guardian LLM to scan incoming content (emails, webhooks) for prompt injection, phishing, and social engineering attacks — beyond the built-in regex patterns.",
  "config.llm_guardian.scan_emails_label": "Scan incoming emails",
  "config.llm_guardian.scan_documents_label": "Scan webhook payloads"
}
